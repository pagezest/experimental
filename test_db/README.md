# DB POC

We have benchmarked multiple sql db servers
- rusqlite
- sqlite

To Start the webserver using the following command

```
cd rusqlite-db
cargo run --release
```

```
cd sqlite-db
cargo run --release
```
